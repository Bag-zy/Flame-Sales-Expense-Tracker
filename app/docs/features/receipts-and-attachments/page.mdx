---
title: Receipts and Attachments
---
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Separator } from '@/components/ui/separator';
import { Paperclip, Upload, Folder, CheckCircle, ArrowRight } from 'lucide-react';

<div className="text-center my-12">
  <h1 className="text-5xl font-bold">Receipts and Attachments</h1>
  <p className="text-xl mt-4 text-muted-foreground">Store supporting documents alongside your financial records for easier audits and reimbursements.</p>
</div>

<Callout type="info" title="Why store receipts?">
  Storing receipts and supporting files alongside your records reduces friction during audits, reimbursements, and month-end close.
</Callout>

<Card className="mt-8">
  <CardHeader>
    <CardTitle className="flex items-center"><Paperclip className="h-6 w-6 mr-2" />Overview</CardTitle>
  </CardHeader>
  <CardContent>
    <p>Receipts and attachments are used to:</p>
    <ul className="list-disc pl-5 mt-2 space-y-1">
      <li>Store proof of payment.</li>
      <li>Capture supplier invoices.</li>
      <li>Add context to expenses and other records.</li>
    </ul>
  </CardContent>
</Card>

<Separator className="my-8" />

<div className="grid md:grid-cols-2 gap-8">
  <Card>
    <CardHeader>
      <CardTitle className="flex items-center"><Upload className="h-6 w-6 mr-2" />Attaching Receipts</CardTitle>
    </CardHeader>
    <CardContent>
      <p>A common workflow:</p>
      <ol className="list-decimal pl-5 mt-2 space-y-1">
        <li>Create or open an <b>Expense</b> record.</li>
        <li>Upload a PDF, image, or other supported file.</li>
        <li>Optionally add a short description.</li>
        <li>Save the record to associate the file with the expense.</li>
      </ol>
    </CardContent>
  </Card>
  <Card>
    <CardHeader>
      <CardTitle className="flex items-center"><Folder className="h-6 w-6 mr-2" />Organizing Attachments</CardTitle>
    </CardHeader>
    <CardContent>
      <p>Good attachment hygiene makes reviews easier:</p>
      <ul className="list-disc pl-5 mt-2 space-y-1">
        <li>Use descriptive file names.</li>
        <li>Attach one invoice per file where possible.</li>
        <li>Avoid uploading the same file multiple times.</li>
      </ul>
    </CardContent>
  </Card>
</div>

<Separator className="my-8" />

<Card>
    <CardHeader>
        <CardTitle className="flex items-center"><CheckCircle className="h-6 w-6 mr-2" />Using Receipts in Workflows</CardTitle>
    </CardHeader>
    <CardContent>
        <p>Receipts support several workflows:</p>
        <ul className="list-disc pl-5 mt-2 space-y-1">
            <li>Manager approvals for expenses.</li>
            <li>Reimbursement for employees.</li>
            <li>External audits and compliance checks.</li>
        </ul>
    </CardContent>
</Card>

<Callout type="warning" title="Retention policies" className="mt-8">
  Check your internal and legal requirements for how long financial documentation must be stored. Make sure Flame’s data retention aligns with those rules.
</Callout>

<Separator className="my-8" />

<div>
  <h2 className="text-2xl font-bold">Related pages</h2>
  <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-4">
    <a href="/docs/features/expenses" className="block p-4 border rounded-lg hover:bg-muted">Expenses <ArrowRight className="inline h-4 w-4 ml-2" /></a>
    <a href="/docs/features/customers-and-vendors" className="block p-4 border rounded-lg hover:bg-muted">Customers and vendors <ArrowRight className="inline h-4 w-4 ml-2" /></a>
    <a href="/docs/features/reports" className="block p-4 border rounded-lg hover:bg-muted">Reports <ArrowRight className="inline h-4 w-4 ml-2" /></a>
  </div>
</div>
